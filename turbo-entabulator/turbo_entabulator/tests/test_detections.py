from unittest import TestCase

import turbo_entabulator

# class TestSomething(TestCase):
#     def test_is_string(self):
#         te = turbo_entabulator.foo()
#         self.assertTrue(isinstance(te, basestring))
